package Movement;

import utility.Point;

public class ToBase implements Movement{

    @Override
    public Point GetNextPoint( Point concurrent ) {
        //Todo: Implement Method to get the next point in way to Base Direction
        return null;
    }
}
